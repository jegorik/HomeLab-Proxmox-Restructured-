# =============================================================================
# PBS Backup Credentials File
# =============================================================================
#
# This file stores ONLY authentication credentials, separated from the main
# config for defense-in-depth. It MUST be owned by root with mode 0600.
#
# Location: /etc/pbs-backup/<profile>.credentials
#
# SECURITY:
#   - Never commit this file to version control.
#   - chmod 0600 and chown root:root are enforced by the management script.
#   - The backup script refuses to run if permissions are too open.
# =============================================================================

# PBS password or API token secret (required)
# For user/password auth: the user's password
# For API token auth:     the token secret value
PBS_PASSWORD="your-password-or-token-secret-here"

# Client-side encryption key (HIGHLY RECOMMENDED)
# Generated with: proxmox-backup-client key create <keyfile>
# Leave empty to disable encryption (NOT RECOMMENDED for sensitive data)
# WARNING: Without this key, backup restoration is IMPOSSIBLE!
#          Store a backup copy in a secure location (password manager, vault, etc.)
ENCRYPTION_KEY=""
